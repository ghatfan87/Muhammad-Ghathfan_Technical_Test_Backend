<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('users', function (Blueprint $table) {
            $table->id();
            $table->string('name');
            $table->string('email')->unique();
            $table->string('password');
            $table->enum("role",['super_admin','customer_service','salesperson','operational','client']);
            $table->enum('sales_type', ['residential', 'commercial', 'both'])->nullable();  
            $table->timestamp('last_assigned')->nullable();
            $table->timestamp('punishment_start')->nullable();
            $table->timestamp('punishment_end')->nullable();
            $table->rememberToken();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('users');
    }
};
